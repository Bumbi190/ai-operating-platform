# Phase 0 Risk Register

| ID | Description | Severity | Likelihood | Evidence | Affected phase | Mitigation | Release implication |
|---|---|---:|---:|---|---|---|---|
| R-001 | Current System Map artifact is stale relative to HEAD and is local/gitignored. | High | High | `apps/web/data/intelligence/system-graph.json` builtAtCommit `d329d93...`; current HEAD `a94f21d`; `.gitignore` ignores artifact path. | Phase 1, Phase 6 | Implement reproducible CI/private-storage delivery before production claim; show stale status until commit is verified. | Cannot release System Map as current architecture truth without artifact delivery/freshness. |
| R-002 | No graph realtime/event envelope exists for Live Operations. | High | High | `IntelligenceGraphClient.tsx` fetches snapshots; no graph `postgres_changes`; no correlation/causation source found. | Phase 3 | Create persisted event contract or verified snapshot+polling contract before operational motion/freeze/replay. | Live Operations cannot be canonical realtime yet. |
| R-003 | Execution Replay lacks required per-step persisted event history. | High | High | Replay tab disabled; `docs/intelligence-graph.md` notes missing per-step event data; no `run_events`/`tool_calls`. | Replay epic / Phase 3 | Add event/order/correlation/causation/attempt/approval/output history first. | Replay must stay disabled. |
| R-004 | SVG main-thread layout may not meet canonical scale/performance budgets at medium/large sizes. | Medium | Medium | `force-layout.ts` O(n^2) repulsion in `useMemo`; response cap 600 nodes; no profiling evidence. | Phase 2, Phase 5 | Profile on canonical datasets; consider worker or precomputed/cached positions if needed. | Do not raise caps or add heavy labels until measured. |
| R-005 | Accessibility is not yet a parallel interface. | High | High | No graph list view; only basic SVG aria/focus in `GraphCanvas.tsx`; no reduced-motion logic. | Phase 4 | Build synchronized list view, keyboard model, reduced motion and screen-reader strategy. | Canonical release blocked until resolved. |
| R-006 | Approval lifecycle in current DB does not match full canonical approval lifecycle. | Medium | Medium | Current CHECK includes pending/approved/rejected/revised/returned/needs_input; canonical mentions requested/pending/approved/rejected/expired/cancelled. | Phase 3 | Decide whether to map existing statuses or extend contract via reviewed migration. | Approval visuals/actions must not imply unsupported statuses. |
| R-007 | Incident model is missing; failed runs may be conflated with incidents. | High | Medium | Graph uses failed run status; no graph incident table/lifecycle found. | Phase 3 | Introduce incident source of truth or keep failures as immutable facts only. | Incident UI cannot be released canonically. |
| R-008 | System Map access boundary is logged-in user, not project-specific. | Medium | Medium if multi-user | `system/route.ts` only checks `supabase.auth.getUser`; docs acknowledge same boundary as `/memory`/`/system`. | Phase 6 | Re-evaluate before non-owner/multi-tenant users; restrict or classify code architecture as platform-global. | Security review gate before wider user base. |
| R-009 | Service-role use in user-facing operations API requires all filtering to remain correct. | High | Medium | `operations/route.ts` uses `createAdminClient`; safety depends on `buildOperationsGraph` and tests. | Phase 3, Phase 6 | Keep allow-list scoping centralized; add route-level/e2e isolation tests; avoid adding unscoped joins. | Any new graph query must pass isolation review. |
| R-010 | Manager context still has a documented global `run_logs` cost read caveat outside graph. | Medium | Medium | `lib/ai/manager.ts` comment says `run_logs` has no project_id and remains global. | Phase 3 / adjacent Manager | Do not use Manager cost context as graph project truth until indirectly scoped. | Prevents reuse of Manager context for multi-tenant graph data. |
| R-011 | Graphify raw artifacts may contain broad repository structure, docs and local manifest data. | Medium | Medium | `graphify-out/manifest.json`, `graph.html`, `graph.json` exist locally and are gitignored; app imports sanitized artifact only. | Phase 6 | Never serve raw `graph.html`; keep importer validation; store sanitized artifact privately. | Raw artifacts must remain local/private. |
| R-012 | Graphify generation/update is manual and may write files unexpectedly. | Medium | High | Graphify skill writes `graphify-out/*`; docs pipeline uses `graphify update .` and import script. | Phase 0, Phase 6 | Document commands only unless explicitly allowed; automate in CI later. | Phase 0 must not run generation. |
| R-013 | Labels can overlap and are not collision-aware. | Medium | High | `GraphCanvas.tsx` uses fixed SVG text below nodes. | Phase 2 | Build label priority/collision system and respect label budgets. | Visual foundation cannot claim canonical label quality. |
| R-014 | Filtering triggers layout recomputation and can break stable mental map. | Medium | Medium | Client filters `nodes`; `GraphCanvas` layout depends on filtered `nodes, edges`; docs note M3 relayout risk. | Phase 2 | Keep base positions stable and dim/filter without relayout where possible. | Requires fix before canonical interaction release. |
| R-015 | No stale/unknown/freshness UI for runtime snapshots. | High | High | Operations API returns `generatedAt`; client does not present freshness/connection state. | Phase 3 | Add freshness metadata, stale thresholds, unknown mapping and visible state. | Cannot show operational motion as live truth. |
| R-016 | No production storage/provisioning for Graphify artifact. | High | High | `docs/intelligence-graph.md` says private Supabase Storage design is decided, not provisioned; no CI workflow files. | Phase 6 | Implement bucket, policies, upload and loader migration in reviewed phase. | Vercel production cannot reliably access local ignored file. |
| R-017 | Canonical node/edge visual language is mostly absent. | Medium | High | All nodes are circles; no diamond approval/tool/output/incident shapes. | Phase 1 | Implement tokenized renderers per canonical taxonomy. | Phase 1 must focus here before more runtime features. |
| R-018 | Reduced motion and quality levels are absent. | Medium | High | SVG `animate` is unconditional for running nodes; no media query or quality state. | Phase 4, Phase 5 | Add reduced-motion detection and quality state before richer motion. | Accessibility/performance gates blocked. |
| R-019 | Tool and memory relations are intentionally omitted because source tables are missing. | Medium | High | `graph-contract.ts` comments exclude `USED_TOOL`, `READ_MEMORY`, `RETRIED_AS`. | Phase 3 / Replay | Add real sources before adding edge types. | Do not visually imply tool/memory paths yet. |
| R-020 | Current tests are unit-focused and do not prove rendered behavior, a11y or device performance. | Medium | High | Graph tests are Vitest contract/builder tests; no graph Playwright/mobile/perf evidence found. | Phase 4-6 | Add browser tests, visual screenshots, a11y/manual test report and profiling. | Canonical release blocked until evidence exists. |

## Five Highest Risks

1. R-002: no graph realtime/event envelope.
2. R-001: stale, local-only System Map artifact.
3. R-005: missing parallel accessibility/list interface.
4. R-009: service-role API depends on perfect server-side scoping.
5. R-003: Replay prerequisites absent.
